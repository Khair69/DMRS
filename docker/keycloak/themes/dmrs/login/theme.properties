# DMRS login theme — extends Keycloak's built-in login theme and layers
# DMRS branding on top via dmrs.css (loaded last so it wins).
parent=keycloak
import=common/keycloak

# Keep the base login.css for layout, then override look-and-feel.
styles=css/login.css css/dmrs.css
