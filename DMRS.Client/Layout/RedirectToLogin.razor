@inject NavigationManager Navigation

@code {
    // Guests are sent to the public welcome screen instead of bouncing straight to the
    // identity provider; from there they can read what DMRS does and choose to log in.
    protected override void OnInitialized()
    {
        Navigation.NavigateTo("welcome");
    }
}
